// This file will store any useful function that can be used anywhere inside of the project

// function that display's a custom message | both in console and as a return
export const showMessage = (message: string) => {
    console.log(message)
    return message
}